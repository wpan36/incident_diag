# The one datasource. Dashboards are M30: a placeholder built against the lab
# alone would be rewritten there.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
