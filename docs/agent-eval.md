# Agent Evaluation

Generated by `make agent-eval` on 2026-09-23.

Model `deepseek-chat`, budget 12 steps / 10 tool calls. Every scenario presents as "checkout requests are bad"; they differ only in why. Scoring is deterministic — see `docs/plans/agent-evaluation.md` for the rule and for what it understates.

## Accuracy

**18 of 24 runs correct (75%).**

| Scenario | Correct | Right service | Stop reason | Steps | Tool calls | Tokens | Seconds |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `payment-latency` | 3/3 | 3/3 | MAX_TOOL_CALLS | 12 | 10 | 51552 / 51189 / 50687 | 20 / 18 / 18 |
| `payment-cpu` | 1/3 | 1/3 | MAX_TOOL_CALLS / COMPLETED | 12 | 10 / 9 / 10 | 53690 / 66280 / 54276 | 26 / 22 / 22 |
| `payment-errors` | 1/3 | 2/3 | MAX_TOOL_CALLS / MAX_STEPS | 12 / 13 / 13 | 10 | 73075 / 80062 / 76514 | 22 / 20 / 22 |
| `checkout-latency` | 3/3 | 3/3 | MAX_TOOL_CALLS / MAX_STEPS | 12 / 12 / 13 | 10 | 51666 / 47911 / 61640 | 22 / 18 / 20 |
| `checkout-cpu` | 3/3 | 3/3 | MAX_STEPS / MAX_TOOL_CALLS | 13 / 12 / 12 | 10 | 90614 / 53850 / 72015 | 24 / 22 / 18 |
| `checkout-errors` | 2/3 | 3/3 | MAX_TOOL_CALLS / MAX_STEPS | 12 / 13 / 12 | 10 | 56322 / 72352 / 50693 | 20 / 22 / 18 |
| `baseline` | 2/3 | 3/3 | MAX_TOOL_CALLS | 12 | 10 | 63853 / 47039 / 54663 | 24 / 20 / 20 |
| `payment-latency-mild` | 3/3 | 3/3 | MAX_STEPS / MAX_TOOL_CALLS | 13 / 12 / 12 | 10 | 77981 / 53752 / 49988 | 22 / 20 / 16 |

Distributions are the values of the individual runs, not a mean: with three runs a mean hides whether the agent is stably right, stably wrong, or flipping a coin.

## Where the budget went

- **23 of 24 runs stopped at a bound** rather than calling `finish` on their own; 1 finished voluntarily and 0 never produced a diagnosis. A bounded run gives its forced diagnosis, not its considered one, so the accuracy above is a lower bound on what a larger budget would give.
- **31 retrieval steps and 239 tool calls**, of which 29 did not return OK. A refused call still spends budget, so the trajectories are worth reading for what was being asked.
- The budget was 12 steps and 10 tool calls, above the shipped default of 8 and 6; `docs/plans/end-to-end-test.md` says why.

## What went wrong

- **`payment-cpu` run 1** — named "checkout-service", want payment-service
- **`payment-cpu` run 3** — named "checkout-service", want payment-service
- **`payment-errors` run 1** — the diagnosis rests on pool exhaust, which is not this fault
- **`payment-errors` run 2** — named "checkout-service", want payment-service
- **`checkout-errors` run 3** — the diagnosis rests on pool exhaust, which is not this fault
- **`baseline` run 2** — invented a root cause: cpu satur

## Trajectories

### `payment-latency`

Expected: `payment-service`, one of `processor`, `pool`, `downstream`, `connection`, and none of `cpu satur`, `cpu throttl`, `timeout is too short`, `raise the timeout`

**Run 1 — correct** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 51552 tokens, 20s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK / REFUSED |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK / REFUSED |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `prometheus_query` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | finish | `finish` | OK |

> **payment-service** — Uncertain, but the evidence points away from the runbook's "client timeout too short" cause (which requires a deployment; none happened today) and toward payment-service being slow: payment-service's request p99 measured 4.95s (step 4), far above its 800ms SLO budget, while checkout abandons calls at only ~2.0/s (step 3). The runbook's alternative cause, payment connection-pool exhaustion, is NOT supported: payment_pool_in_use was 10 of 20 and the pool wait histogram showed every one of 639 observations in the lowest bucket (sum 0.0008s) (step 9), and the processor's own call rate was ~2.1/s (step 11), so the pool was not the bottleneck. I could not complete the checkout-vs-payment abandonment gap comparison or confirm which calls in payment-service are slow, so the exact mechanism of payment-service's latency is not established.

**Run 2 — correct** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 51189 tokens, 18s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `prometheus_query` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | finish | `finish` | OK |

> **payment-service** — The 504s on checkout-service `POST /orders` are caused by payment-service being slow, not by checkout abandoning completed calls: payment-service's own p99 request latency is ~4.95s against its published 800ms budget (step 3/4), and its latency histogram shows ~55% of requests (2.33636 - 1.03956 of 2.33636/s) landing above 500ms with nothing below 500ms at all (step 6). Consequently checkout's client timeout is expiring on genuinely slow downstream calls — checkout's `checkout_payment_client_timeouts_total` is ~2.28/s (step 2) against payment-service's ~2.34 req/s (step 6), i.e. essentially every payment call is being abandoned (step 11 shows POST /orders 504 at ~2.41/s and no successful orders), but that is a symptom of the callee's degradation rather than a misconfigured client timeout. The payment-service pool hypothesis is not supported: the pool is not saturated (9 in use, step 8) and average wait time is ~1.2µs (step 9), so this is not the March pool-exhaustion pattern; payment-service latency itself is the bottleneck. Root cause of payment-service's own latency was not established within the step budget.

**Run 3 — correct** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 50687 tokens, 18s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK / REFUSED |
| 8 | tool_call | `prometheus_query` | OK / REFUSED |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `prometheus_query` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | finish | `finish` | OK |

> **payment-service** — payment-service itself is slow, not checkout's timeout: payment_processor_latency_seconds p99 is ~4.96s (step 10) and payment-service's own /charge latency p99 is ~4.96s (step 3), far above its 800ms p99 budget, while checkout abandons 2.5 calls/s (step 2) at CHECKOUT_PAYMENT_TIMEOUT. checkout's own p99 (2.485s, step 5) is consistent with waiting on payment. Payment traffic appears elevated (752 requests in the window with 1909s cumulative latency, step 11) and payment_pool_wait_seconds exists (step 9) but the pool gauges/waits in this window were not resolved before the budget ran out, so the exact mechanism inside payment-service (pool exhaustion as in postmortem 01M35HQ9K69A5S72EMN774JYYA, versus a slow card processor) is not yet confirmed — payment_pool_usage is only 1 with size 1 as measured, i.e. the available series are too aggregated to tell. Note this is NOT the May 2026 pattern (postmortem 01M35HQ9KM0EZBSH7BW7ZPCR5V): no deploy occurred today and the downstream itself is slow.

### `payment-cpu`

Expected: `payment-service`, one of `cpu`, `saturat`, `throttl`, and none of `card processor is slow`, `processor latency is high`

**Run 1 — wrong** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 53690 tokens, 26s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `read_service_logs` | OK |
| 11 | tool_call | `read_service_logs` | OK |
| 12 | finish | `finish` | OK |

> **checkout-service** — Not settled. checkout-service's p99 on POST /orders is elevated (~2.24s overall, 2.37s on the /orders route only, while /orders/:id is 0.005s and checkout's own CPU is ~0.003 cores), which points at something on the payment call path rather than checkout CPU. But the two runbook candidates this symptom usually splits into are both contradicted as measured: checkout_payment_client_timeouts_total is 0 (step 5), so checkout is not abandoning calls at its client timeout, and payment-service's own p99 is 0.79s, right at its 800ms budget rather than badly degraded (step 4). The metric that would separate "payment-service genuinely slow" from "checkout waiting on something else in the payment path" was not yet checked, so I cannot name the root cause.

Marked wrong: named "checkout-service", want payment-service

**Run 2 — correct** (`SUCCEEDED`/`COMPLETED`, 12 steps, 9 tool calls, 66280 tokens, 22s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `read_service_logs` | OK |
| 7 | tool_call | `read_service_logs` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `prometheus_query` | OK / REFUSED |
| 10 | tool_call | `prometheus_query` | OK |
| 11 | retrieve | `search_knowledge` | OK |
| 12 | finish | `finish` | OK |

> **payment-service** — Checkout is degraded by payment-service, not by checkout's own resources. payment-service's own p99 over the last 5m is 817ms (step 2), above its 800ms budget, and checkout's p99 is 2.23s (step 3) — consistent with the runbook's rule that an elevated payment p99 means this is a payment-side incident, not a checkout-side one. Checkout's own CPU is flat (0.0034 cores/s, step 5), which rules out the serialization/CPU diagnosis, and client timeouts on the payment call are at zero (step 4), which rules out the 2026-05 misconfiguration where CHECKOUT_PAYMENT_TIMEOUT was shortened below the callee's budget. Logs show no warnings or errors since 00:30 and POST /orders still returning 201 with durations mostly 400-1250ms (step 7) — a latency degradation, not a timeout/error storm. I could not settle the mechanism inside payment-service (card-processor pool exhaustion as in 2026-03 vs. something else); payment_pool_in_use and payment_pool_wait_seconds were not probed before the budget ran out.

**Run 3 — wrong** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 54276 tokens, 22s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `prometheus_query` | OK / REFUSED |
| 10 | tool_call | `read_service_logs` | OK |
| 11 | tool_call | `read_service_logs` | OK |
| 12 | finish | `finish` | OK |

> **checkout-service** — Checkout's own request latency is the anomaly, not payment-service's. payment-service is behaving within budget (p99 ~0.83s vs its 800ms SLO, pool in-use ratio 0, CPU ~0.67 cores), while checkout-service p99 is 2.25s against its 1500ms objective, and the only timeouts visible (checkout_payment_client_timeouts_total = 0) are zero. Checkout's measured payment client latency distribution is capped at 1.0s (buckets end at le=1.0 -> 710, le=2.5 -> 806, +Inf = 806, sum/count = 0.68s average), i.e. a tight distribution consistent with normal payment calls plus checkout's own processing time on top. This matches the runbook's note that order serialization is CPU-heavy and scales with basket size, so latency originates inside checkout-service rather than in payment-service. I could not confirm this fully: container CPU metrics for checkout-service did not exist in Prometheus (container_cpu_usage_seconds_total / container_cpu_cfs_throttled_seconds_total matched no series) and checkout-service emitted no WARN-or-above logs in the incident window, so the exact in-process cause (owner CPU saturation on large-basket serialization vs something else on checkout's own path) is not settled by measurement.

Marked wrong: named "checkout-service", want payment-service

### `payment-errors`

Expected: `payment-service`, one of `5xx`, `503`, `error rate`, `errors`, `rejecting`, `shedding`, and none of `cpu satur`, `pool exhaust`, `processor latency`

**Run 1 — wrong** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 73075 tokens, 22s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `http_probe` | OK |
| 3 | tool_call | `http_probe` | OK |
| 4 | tool_call | `read_service_logs` | OK |
| 5 | tool_call | `http_probe` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK / REFUSED |
| 9 | tool_call | `prometheus_query` | OK / REFUSED |
| 10 | tool_call | `prometheus_query` | OK / REFUSED |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | finish | `finish` | OK |

> **payment-service** — checkout-service's POST /orders is failing 502s immediately (duration_ms 0-1), and the checkout logs state the cause directly: "the charge failed error=payment-service answered 503" — payment-service itself is returning 503 to checkout's authorization calls, so the failure is a downstream error on payment-service's side, not a checkout-side client timeout. This is distinct from the two documented checkout-504 causes (2026-05 timeout too short, 2026-03 pool exhaustion). I did not get far enough to prove why payment-service returns 503: payment-service /health reports 200, and payment pool metrics exist (payment_pool_in_use, payment_pool_size) but I could not read their values, so I cannot yet say whether this is pool saturation, a dependency (card processor) outage, or deliberate shedding. The specific cause of the 503 is unconfirmed.

Marked wrong: the diagnosis rests on pool exhaust, which is not this fault

**Run 2 — wrong** (`SUCCEEDED`/`MAX_STEPS`, 13 steps, 10 tool calls, 80062 tokens, 20s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | retrieve | `search_knowledge` | OK |
| 3 | tool_call | `http_probe` | OK |
| 4 | tool_call | `http_probe` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK / REFUSED |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `prometheus_query` | OK / REFUSED |
| 11 | tool_call | `prometheus_query` | OK / REFUSED |
| 12 | tool_call | `prometheus_query` | OK |
| 13 | finish | `finish` | OK |

> **checkout-service** — I could not confirm a root cause within this run. Checkout-service and payment-service both report healthy (/health 200, 0ms), and checkout's payment-client metrics show 716 completed calls with zero client timeouts (checkout_payment_client_timeouts_total = 0), which rules out the documented "client timeout below payment-service's p99" cause from the May postmortem and the pool-exhaustion cause from the March postmortem. But I never obtained the smoking gun: I could not read per-status-code request counts (the estate's metrics do not expose http_requests_total by status for checkout-service; it only exposes duration buckets/histograms), so I have no measurement of which 5xx checkout returns (500 vs 503 vs 504) or of payment-service's error rate right now. The incident's key detail — immediate 5xx rather than after a wait — is inconsistent with the timeout-based runbook causes, and the checkout payment-client latency histogram shows the whole distribution under ~100ms with no long tail, so something other than raw latency is failing a share of requests. An immediately-returning 5xx on a fraction of POST /orders most likely comes from an error path in checkout's payment call (connection/pool rejection, a client-side error mapped to 5xx, or an error-rate dependency such as the MySQL ledger behind payment-service) that I did not get to measure. The observations support "not the known timeout/pool causes" but not a specific alternative, so this is an honest partial answer.

Marked wrong: named "checkout-service", want payment-service

**Run 3 — correct** (`SUCCEEDED`/`MAX_STEPS`, 13 steps, 10 tool calls, 76514 tokens, 22s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | retrieve | `search_knowledge` | OK |
| 9 | tool_call | `read_service_logs` | OK |
| 10 | tool_call | `read_service_logs` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | tool_call | `prometheus_query` | OK |
| 13 | finish | `finish` | OK |

> **payment-service** — payment-service is returning immediate 503s for a share of POST /charge calls, which checkout-service surfaces as 502s on POST /orders (logs: "the charge failed error=payment-service answered 503", duration_ms=0-1, i.e. instant rather than a timeout). This is NOT the common runbook cause of checkout-side client timeouts: checkout_payment_client_timeouts_total is 0, checkout's payment client latency is healthy (~41ms mean), and payment-service's processor/pool metrics are clean (pool_in_use 0/20, pool wait sum 1ms). The error-rate runbook documents 503 + log-shedding as the signature of payment-service protecting itself, but I could not confirm which protection is active: the flag/metric query returned no series, and checkout's 5xx mix at the same instant is split roughly evenly between 502 (0.50/s) and 503 (0.51/s), so the 503-flag diagnosis is plausible but unproven. Also no slow requests anywhere, contradicting a latency/pool-exhaustion cause.

### `checkout-latency`

Expected: `checkout-service`, one of `checkout`, and none of `cpu satur`, `cpu throttl`, `payment-service is slow`

**Run 1 — correct** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 51666 tokens, 22s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `http_probe` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `prometheus_query` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | finish | `finish` | OK |

> **checkout-service** — Slow checkout is not explained by any downstream dependency or by checkout's own CPU, all of which are healthy: payment-service p99 is ~0.1s and its client-side latency as measured by checkout is ~0.1s (mean ~51ms), process CPU on both services is near zero, payment's pool is idle (in_use 0/20, wait sum 0.95ms over 643 requests), and checkout has recorded zero payment client timeouts. Yet checkout's own p99 for /orders and /orders/:id is ~4.95s while /health served in 2ms. So latency is confined to the order routes, is not coming from payment, and is not caused by a deployment (none shipped today). A serializer/order-path specific problem inside checkout — of the kind the runbook describes as CPU-heavy/CPU-saturation-free-when-uniform — or another local dependency used only by the order routes is not yet identified; the evidence does not distinguish them, so the exact root cause remains unproven.

**Run 2 — correct** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 47911 tokens, 18s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `prometheus_query` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | finish | `finish` | OK |

> **checkout-service** — Checkout's own latency is elevated across both of its data routes (p99 ≈ 4.96s on both /orders and /orders/:id), while payment-service's /charge p99 is 99.5ms — far inside its published 800ms budget. Because the slowdown is uniform across routes and payment-service is not slow, this is not a downstream timeout cause; the runbook's third hypothesis (checkout's own CPU) is also unsupported, since checkout-service's CPU use is only ~0.0055 cores/s versus payment-service's ~0.0023, i.e. no saturation on a single replica. The evidence therefore does not yet identify the root cause: it rules out payment-service slowness and the low-CHECKOUT_PAYMENT_TIMEOUT misconfiguration (no payment latency to abandon), and rules out checkout CPU saturation, but the mechanism making both routes take ~5s (e.g. the MySQL orders table / basket lookups, or lock contention in checkout) was not measured.

**Run 3 — correct** (`SUCCEEDED`/`MAX_STEPS`, 13 steps, 10 tool calls, 61640 tokens, 20s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | retrieve | `search_knowledge` | OK |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `prometheus_query` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | tool_call | `prometheus_query` | OK |
| 13 | finish | `finish` | OK |

> **checkout-service** — Unresolved, but the evidence rules out the two causes the runbook would lead you to first. Checkout's own p99 is ~4.95s on both /orders and /orders/:id while payment-service's p99 is only ~100ms and its CPU is low (0.0026 cores/s), and checkout_payment_client_timeouts_total is 0 — so this is neither checkout CPU saturation nor the classic client-timeout-below-callee-budget failure, and it is not payment-service slowness. The one measured fact that is anomalous is the checkout→payment client latency histogram: all 745 sampled calls (38.16s total) fall into the 0.1s-or-under bucket, i.e. checkout's payment calls are completing normally (~51ms average), yet the request is still taking ~5 seconds. That means the seconds are being spent somewhere in checkout that is not the payment call — most plausibly the MySQL orders write / Kafka order.placed publish path (checkout is the only writer of the orders table, and p99 is elevated even on GET /orders/:id, which need not write) — but no metric in this run was available to confirm which. /health is unaffected (5ms), so this is confined to the request path, and uniformity across the two order routes cannot distinguish CPU from a dependency with the data seen here.

### `checkout-cpu`

Expected: `checkout-service`, one of `cpu`, `saturat`, `throttl`, and none of `payment-service is slow`, `pool exhaust`

**Run 1 — correct** (`SUCCEEDED`/`MAX_STEPS`, 13 steps, 10 tool calls, 90614 tokens, 24s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `http_probe` | OK |
| 5 | tool_call | `read_service_logs` | OK |
| 6 | retrieve | `search_knowledge` | OK |
| 7 | tool_call | `read_service_logs` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `prometheus_query` | OK |
| 11 | tool_call | `read_service_logs` | OK |
| 12 | tool_call | `read_service_logs` | OK |
| 13 | finish | `finish` | OK |

> **checkout-service** — A latency fault-injection ("fault enabled delay_ms=2500 jitter_ms=250 kind=latency") was enabled on checkout-service at 2026-09-22T23:03:53Z, ~5 minutes before the incident was filed. This injected ~2.5s of delay on checkout's request path and is what made POST /orders and GET /orders/{id} take seconds; it is not a real downstream or CPU problem. payment-service is healthy (p99 0.0995s), so the runbook's "downstream" and "CPU" diagnoses do not apply. Caveat: the aggregate service p99 (0.24s) and route-level medians are far below the reported "seconds", and sampled request logs after 23:07 show only 60-100ms on POST /orders and 0ms on GET /orders/:id, so the injected latency and the customer-visible symptom are not fully consistent — the latency injection may have stopped or be applied to only a subset of traffic/instances. Confidence in "chaos injection is the trigger" is high; confidence that it fully explains the reported slowness is moderate.

**Run 2 — correct** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 53850 tokens, 22s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `prometheus_query` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | finish | `finish` | OK |

> **checkout-service** — The observations I gathered do not reproduce the reported symptom and therefore do not support a confident root cause. Every metric I read shows checkout-service fast and healthy: p99 for GET /orders/:id is ~5ms and flat, p99 for POST /orders is ~245ms, well under the 1500ms SLO, and flat across the reported window (23:08–23:15Z); checkout's own CPU rate is ~0.6 cores; and payment-service's own p99 on /charge is ~100ms, far below its 800ms budget. So neither of the checkout runbook's causes for checkout latency matches what I measured for this incident: the CPU-saturation branch is contradicted by the low process_cpu value of 0.6 and by GET /orders/:id being fast (the runbook's distinguishing signal is that CPU saturation raises latency on every endpoint at once), and the "client timeout abandoned calls / slow downstream" branch is contradicted by payment-service being healthy (p99 ~100ms vs 800ms budget) and by the absence of elevated latency or 504s on POST /orders. My attempt to check the 504 rate and request status mix directly failed because the metric-name queries I used returned no series, so I could not confirm or exclude a 504 rate or a timeout-abandonment gap. The reported "seconds" latency may be intermittent/bursty and averaged away in a 5-minute rate window, may be confined to a route or basket-size cohort I did not break out, or may sit in a dependency I never measured (MySQL orders table, Kafka event publish, or serialization for very large baskets).

**Run 3 — correct** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 72015 tokens, 18s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `read_service_logs` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `prometheus_query` | OK / REFUSED |
| 10 | tool_call | `prometheus_query` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | finish | `finish` | OK |

> **checkout-service** — I could not confirm the reported symptom. checkout-service's own p99 over the incident window is ~240ms (per-route: POST /orders 245ms, GET /orders/:id 5ms) and the sampled request logs show POST /orders completing in 51-100ms with 201s and GETs at ~0ms, i.e. sub-second, not "seconds" — and payment-service's p99 is only 99.5ms, far below its 800ms budget, so the runbook's two leading causes (client timeout below the callee's SLO, or a slow payment-service) are both ruled out by the measurements. CPU use is moderate (0.69 cores/s) and the latency is not visible on /orders/:id, which argues against the runbook's other candidate (uniform CPU saturation from serialization). What the data does show is that the 240ms p99 is flat across the whole window rather than rising over the last fifteen minutes, so either the complaint is about a different signal/path than the ones I sampled, or the degradation is recent enough that the 5-minute averages have already masked it. This is an unresolved investigation, not a confirmed diagnosis.

### `checkout-errors`

Expected: `checkout-service`, one of `5xx`, `503`, `error rate`, `errors`, `rejecting`, `shedding`, and none of `cpu satur`, `pool exhaust`

**Run 1 — correct** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 56322 tokens, 20s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK / REFUSED |
| 4 | tool_call | `prometheus_query` | OK / REFUSED |
| 5 | tool_call | `prometheus_query` | OK / REFUSED |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `prometheus_query` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | finish | `finish` | OK |

> **checkout-service** — Checkout-service is returning fast HTTP 503s (not 504 timeouts) on ~18% of POST /orders (110 of 599) and also on ~19% of GET /orders/:id — so the failures are not confined to the payment path and are not the classic "CHECKOUT_PAYMENT_TIMEOUT too short" pattern described in the runbooks (those produce 504s after a wait). The failures are immediate 503s across routes that touch the orders store, which points at an internal resource dependency of checkout (MySQL orders table / connection pool or replica capacity) rather than at payment-service or at checkout's own CPU. I could not confirm the exact sub-cause: the metric series for the payment client timeout counter and the CPU metrics were not retrieved before the step budget ran out, so the "why" of the 503s is still unverified.

**Run 2 — correct** (`SUCCEEDED`/`MAX_STEPS`, 13 steps, 10 tool calls, 72352 tokens, 22s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `http_probe` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK / REFUSED |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | retrieve | `search_knowledge` | OK |
| 10 | tool_call | `read_service_logs` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | tool_call | `prometheus_query` | OK / REFUSED |
| 13 | finish | `finish` | OK |

> **checkout-service** — Checkout-service is failing a growing share (~10-25%) of POST /orders and GET /orders/:id with 503 returned immediately (duration_ms=0), not a timeout: 100 of 436 GET /orders/:id requests and 123 of 536 POST /orders (plus 536 successful) are 503, and the 503 rate is climbing. The two documented latency causes do not fit: there are zero checkout_payment_client_timeouts_total (step 11), so this is not the CHECKOUT_PAYMENT_TIMEOUT misconfiguration of the 2026-05 postmortem, and payment client latency maxes at 100ms, not the payment-service pool exhaustion of the 2026-03 postmortem. Zero-latency 503s plus an affected read path point to checkout's own local dependency (orders-table MySQL access or its connection pool/local capacity) being unavailable or exhausted, but I could not confirm which — my read-only budget ran out before I could query checkout's pool/DB metrics or the MySQL health, so the exact local failure mode is a hypothesis, not a measurement.

**Run 3 — wrong** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 50693 tokens, 18s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK / REFUSED |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK / REFUSED |
| 8 | tool_call | `prometheus_query` | OK / REFUSED |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `prometheus_query` | OK / REFUSED |
| 11 | tool_call | `prometheus_query` | OK / REFUSED |
| 12 | finish | `finish` | OK |

> **checkout-service** — I could not establish a confident root cause. The incident description (immediate 5xx on a share of POST /orders, nothing deployed today) matches two documented, opposite failure modes: checkout-service's own client timeout CHECKOUT_PAYMENT_TIMEOUT being set below payment-service's published 800ms p99 budget, or a downstream collapse such as payment-service connection-pool exhaustion. My checks did not discriminate between them. `checkout_payment_client_timeouts_total` was 0 and the payment client's measured latency was low (mean ~51ms over 644 samples: sum 32.85s / count 644, step 3), which argues against the timeout-revert scenario — but "immediately rather than after a wait" is more consistent with a fast-failing dependency (upstream connection refused / pool rejection / DNS) than with a latency timeout, and I never measured the checkout error breakdown or payment-service's pool, error rate and p99. Prometheus is also emitting metrics with an empty label set and intermittently rejecting or truncating queries, so my metric evidence is weaker than I would like.

Marked wrong: the diagnosis rests on pool exhaust, which is not this fault

### `baseline`

Expected: nothing is wrong; a diagnosis naming `pool exhaust`, `cpu satur`, `processor is slow`, `error rate is high` is an invented root cause

**Run 1 — correct** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 63853 tokens, 24s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `read_service_logs` | OK |
| 9 | tool_call | `read_service_logs` | OK |
| 10 | tool_call | `read_service_logs` | OK |
| 11 | tool_call | `http_probe` | OK |
| 12 | finish | `finish` | OK |

> **checkout-service** — No production problem is currently demonstrated. Every measurement taken during this run is at baseline: checkout-service p99 is 0.099s and payment-service p99 is 0.0995s (step 2), process CPU on both is ~0.2–0.3% of a core (step 3), the checkout→payment client timeout counter is 0 and the client latency p99 is ~0.1s (steps 4, 5), all sampled HTTP responses are 200/201 with no 5xx (step 7), and /health returns 200 in 2ms (step 11). The only anomaly in the run is a small burst of duration_ms=0 503s on /orders and /orders/:id — 17212 WARN/ERROR records at some point since 20:00Z, with the last 200 clustered at 23:11:58–23:12:50 (step 8) and the tail at 23:14:08–23:14:32 (step 9) — and zero in the 23:15:00 onward window queried (step 10). A zero-duration 503 is an upstream/load-balancer fast-fail, not a slow request, so this is not the "slow checkout" the report describes. Caveat: the 503 burst overlapped the reporting window and could not be traced to a cause before the step budget ran out, and the metric queries for the client-timeout and CPU series returned values that look suspiciously low (0.0033 cores/s), suggesting the scrape may be sparse or these may not be the production series — so "nothing is wrong" is supported but not airtight.

**Run 2 — wrong** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 47039 tokens, 20s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `http_probe` | OK |
| 10 | tool_call | `prometheus_query` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | finish | `finish` | OK |

> **checkout-service** — There is no evidence of an actual incident. Everything measured on checkout-service is healthy: its request p99 is ~99ms (far below the 3s alert threshold), process CPU is ~0.4% of a core, the payment client timeout counter is 0 (flat at zero over 30m, so checkout is not abandoning calls), and the service's own /health returns 200 instantly. payment-service's p99 is ~100ms, well inside its published 800ms budget. The single slow checkout the customer reported cannot be reproduced or corroborated in any signal checked — it looks like an isolated tail event, not a systemic slowdown. One caveat: several queries matched no series (5xx rate by code/path, cAdvisor container CPU), so per-route error/duration breakdowns and container-level CPU saturation could not be confirmed; the aggregate p99 and process CPU I did measure argue against both of the runbook's candidate causes (downstream timeouts vs. checkout CPU saturation).

Marked wrong: invented a root cause: cpu satur

**Run 3 — correct** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 54663 tokens, 20s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `read_service_logs` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | finish | `finish` | OK |

> **checkout-service** — No fault found in checkout-service. I could not reproduce any degradation: its p99 latency is ~99ms (well under the 3s alert threshold), payment-service's p99 is equally ~99ms, checkout's client-timeout counter rate is exactly 0 over the last 30m, checkout CPU is idle (~0.004 cores), and every /orders request in the log window returned 201 with duration_ms ~51. The customer's one slow checkout is not visible as a systemic incident; if it was real it was a single anomalous request, not a condition currently affecting the service. Note the metric series likely lacks histogram buckets (histogram_quantile returns a constant 0.0995 identical for both services), so the p99 figures should be confirmed against per-route latency before being treated as precise.

### `payment-latency-mild`

Expected: nothing is wrong; a diagnosis naming `pool exhaust`, `cpu satur`, `error rate is high`, `outage` is an invented root cause

**Run 1 — correct** (`SUCCEEDED`/`MAX_STEPS`, 13 steps, 10 tool calls, 77981 tokens, 22s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `read_service_logs` | OK |
| 10 | retrieve | `search_knowledge` | OK |
| 11 | tool_call | `prometheus_query` | OK / REFUSED |
| 12 | tool_call | `prometheus_query` | OK |
| 13 | finish | `finish` | OK |

> **checkout-service** — No fault found in checkout-service: it is healthy right now. Its own process CPU is negligible (0.001-0.002 cores, step 12, 5), its p99 http latency is ~495ms (step 2), no 504 failures or client timeouts are being produced (checkout_payment_client_timeouts_total = 0, step 4), and payment-service's own p99 is normal at ~497ms (step 7, well inside its 800ms budget). The POST /orders histogram (step 8) shows the whole tail captured under the 0.5s bucket (632 of 632 requests) with a mean of 348ms, and recent logs (step 9) show all 201s with durations of 250-450ms — slower than a tight baseline but consistently under the 1500ms SLO and producing no errors. So the customers' "feels slow" report is explained by a mildly elevated but fully successful response time, not by either of the runbook's failure modes (timeout misconfiguration or CPU saturation). Because the server only returned two CPU samples covering 23:20-23:25 and no historical series at all, I cannot confirm whether there was a distinct earlier degradation this afternoon or what shape it had; the elevated-but-healthy picture may partly be an artifact of the coarse, incomplete metric history available to me.

**Run 2 — correct** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 53752 tokens, 20s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK |
| 7 | tool_call | `prometheus_query` | OK |
| 8 | tool_call | `prometheus_query` | OK / REFUSED |
| 9 | tool_call | `prometheus_query` | OK / REFUSED |
| 10 | tool_call | `prometheus_query` | OK |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | finish | `finish` | OK |

> **checkout-service** — No evidence of a real problem found: checkout-service is healthy in every signal I could check. The p99 of POST /orders is ~497ms, well under the 1500ms SLO (steps 2, 3, 5); mean latency is ~346ms (step 6); checkout_payment_client_timeouts_total is 0 and all 693 payment calls completed, none above the 500ms bucket boundary in the payment client histogram (step 10); process CPU is essentially idle at ~0.004 cores (step 11); and there are no 5xx/504 status series at all (steps 4, 10). The "slowness" is not reproduced. Caveat: the metrics backend only returned a 2-point window (23:20 and 23:25, step 7), so I could not reconstruct the afternoon's latency curve, and I never reached a logs or health check to rule out a transient earlier in the day. The one thing that would deserve attention is the payment client latency distribution: the final bucket is le=0.5 with all 693 calls in it, so it cannot distinguish normal tail (600-700ms, as the runbook describes) from a timeout cliff — but since timeouts are 0 and no 504s exist, nothing is currently being abandoned. Best available answer: no active incident, inconclusive only for the afternoon window.

**Run 3 — correct** (`SUCCEEDED`/`MAX_TOOL_CALLS`, 12 steps, 10 tool calls, 49988 tokens, 16s)

| Step | Action | Tool | Status |
| --- | --- | --- | --- |
| 1 | retrieve | `search_knowledge` | OK |
| 2 | tool_call | `prometheus_query` | OK |
| 3 | tool_call | `prometheus_query` | OK |
| 4 | tool_call | `prometheus_query` | OK |
| 5 | tool_call | `prometheus_query` | OK |
| 6 | tool_call | `prometheus_query` | OK / REFUSED |
| 7 | tool_call | `prometheus_query` | OK / REFUSED |
| 8 | tool_call | `prometheus_query` | OK |
| 9 | tool_call | `prometheus_query` | OK |
| 10 | tool_call | `prometheus_query` | OK / REFUSED |
| 11 | tool_call | `prometheus_query` | OK |
| 12 | finish | `finish` | OK |

> **checkout-service** — No fault found so far. checkout-service p99 (step 2) is 0.495s, well inside its 1500ms SLO and far below the 3s alert threshold; payment-service p99 is 0.4975s (step 3), normal against its 800ms budget; checkout's process CPU rate is ~0.0044 cores/s (step 4), i.e. idle, ruling out the serialization/CPU-saturation cause; and checkout_payment_client_timeouts_total is flat at 0 (step 9), so no calls are being abandoned the way the 2026-05 postmortem describes. Without raw request-rate/count or per-route latency data (steps 5-7 show the request counters are not exposed under the names the runbook assumes), I cannot demonstrate a real degradation — the "slower" reports are equally consistent with a handful of normal, sub-second-slower tail requests that never breached an alert. This is a partial answer: nothing visible in the current metrics points to a genuine incident.

